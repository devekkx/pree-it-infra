# Local development conveniences — never used in CI/prod
services:
  postgres:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  grafana:
    ports:
      - "3001:3000"

  garage:
    ports:
      - "9000:9000"
      - "3903:3903"

  gateway:
    volumes:
      - ../preeit-gateway:/app

  auth:
    volumes:
      - ../preeit-auth:/app
